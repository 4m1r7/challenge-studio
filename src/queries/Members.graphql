query Members {
  members(first: 100) {
    edges {
      node {
        id
        title
        slug
        featuredImage {
          node {
            sourceUrl(size: MEDIUM_LARGE)
          }
        }
        memberFields {
          oldMember
          position
        }
      }
    }
  }
}
