query ContactPage {
  pageBy(uri: "contact") {
    id
    content
    contactPageFields {
      maps {
        darkModeMap {
          mediaItemUrl
        }
        lightModeMap {
          mediaItemUrl
        }
      }
      googleMapsLink
    }
  }
}
