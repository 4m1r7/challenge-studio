query ContactPage {
  pageBy(uri: "contact") {
    id
    content
    contactPageFields {
      darkModeMap {
        mediaItemUrl
      }
      lightModeMap {
        mediaItemUrl
      }
    }
  }
}
