query AllProjects {
  projects {
    edges {
      node {
        slug
        id
        title
        uri
        featuredImage {
          node {
            sourceUrl(size: MEDIUM_LARGE)
          }
        }
        projectFields {
          scale
          status
          type
          year
        }
      }
    }
  }
}
